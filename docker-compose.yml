version: "3"

services:
  api:
    build: ./apps/api
    ports:
      - "5000:5000"
    env_file:
      - .env

  ai:
    build: ./apps/ai
    ports:
      - "8000:8000"

  web:
    build: ./apps/web
    ports:
      - "3000:3000"
